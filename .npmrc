# Ignore peer dependency conflicts (npm equivalent of strict-peer-dependencies=false)
legacy-peer-deps=true

# Cache location
cache=.npm-cache

# Audit settings
audit=true
audit-level=moderate

# Use exact versions in package-lock.json
save-exact=false

# Increase timeout for slow networks
fetch-timeout=60000
